{% extends 'base.html.twig' %}

{% block body %}

<h1>
    Modifier une personne
</h1>
    {{ form(formulaire) }}
    <a class="btn btn-warning" href="{{ path("personne") }}">Retour à la liste</a>
{% endblock %}
